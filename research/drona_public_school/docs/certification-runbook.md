# Certification Runbook

Certification is enabled by default for every course.

Default mode uses Moodle core badges, so no plugin is required. If the school later installs a PDF certificate plugin, update `templates/certification/certificate_policies.csv` and `years/<year>/course_certificates.csv`.

Each course must have one row in `course_certificates.csv`.
